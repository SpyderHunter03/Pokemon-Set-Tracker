# runtime state (user accounts, synced collections)
data/
.test-data/

# generated card database (rebuild with scripts/build-data.js)
public/cdn/

# dependencies (sharp is optional, installed ad hoc)
node_modules/
package-lock.json

# misc
*.zip
*.log
.DS_Store

# R2 credentials for the publish script (never commit)
r2.env
